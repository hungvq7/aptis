# Generate a random value for local development, e.g.: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Required in production; a dev-only fallback is used automatically when unset locally.
SESSION_SECRET=
